import {RatelimitActionProvider} from '../../providers';

describe('RatelimitActionProvider', function () {
  it('should work', function () {
    expect(RatelimitActionProvider).toBeTruthy();
  });
});
