{
  "name": "@bleco/sanitization",
  "description": "A LoopBack 4 project for securely filtering sensitive fields from API responses, featuring custom decorators and global interceptors to enhance data privacy and security in RESTful services.",
  "version": "0.2.2",
  "keywords": [],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=16"
  },
  "author": "ty",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "nb-tsc -b",
    "clean": "del 'bleco-data-santitizer*.tgz' 'dist/*' '*.tsbuildinfo' 'package'",
    "pretest": "yarn run clean && yarn run build",
    "test": "yarn run pretest && yarn run jest",
    "jest": "jest"
  },
  "peerDependencies": {
    "@loopback/core": "^5.1.3"
  },
  "devDependencies": {
    "@nutol/build": "^0.2.18",
    "@nutol/eslint-config": "^0.2.13",
    "@nutol/ts-config": "^0.2.2",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.5",
    "del-cli": "^5.1.0",
    "jest": "^29.7.0"
  },
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ]
}
