{
  "name": "@bleco/query",
  "description": "A sql query with INNER JOIN supported for loopback-next based on knex",
  "version": "0.6.0",
  "keywords": [
    "bleco",
    "looback",
    "loopback-extension",
    "sql",
    "builder",
    "query",
    "knex",
    "join",
    "inner",
    "innerjoin",
    "relation",
    "relations",
    "joinrelation",
    "joinrelations",
    "join-relation",
    "join-relations",
    "find",
    "findone",
    "findall",
    "all",
    "one",
    "count",
    "select",
    "filter",
    "where"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=14"
  },
  "author": "TY",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "nb-tsc",
    "clean": "nb-clean 'bleco-query*.tgz' 'dist/*' '*.tsbuildinfo' 'package'",
    "pretest": "yarn run clean && yarn run build",
    "test": "yarn run pretest && yarn run jest",
    "jest": "jest",
    "jest:ci": "jest --collectCoverage --coverageDirectory=\"./coverage\" --reporters=default --reporters=jest-junit --watchAll=false"
  },
  "peerDependencies": {
    "@loopback/core": "^4.0.0",
    "@loopback/filter": "^3.0.0",
    "@loopback/repository": "^5.0.0"
  },
  "dependencies": {
    "debug": "^4.3.4",
    "knex": "^2.0.0",
    "tily": "^2.16.1",
    "ts-essentials": "^9.1.2"
  },
  "devDependencies": {
    "@loopback/core": "^4.0.0",
    "@loopback/filter": "^3.0.0",
    "@loopback/repository": "^5.0.0",
    "@nutol/build": "^0.2.6",
    "@nutol/eslint-config": "^0.2.2",
    "@types/jest": "^27.5.1",
    "@types/node": "^17.0.33",
    "loopback-connector-postgresql": "^5.5.0",
    "loopback-connector-sqlite3e": "^3.2.0",
    "pgmock2": "^2.1.3"
  },
  "files": [
    "README.md",
    "README-CN.md",
    "dist",
    "src",
    "!*/__tests__"
  ]
}
